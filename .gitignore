node_modules
dist
dist-ssr
*.local
.DS_Store
.vite

# API 인증키 — 절대 커밋하지 않는다
.env
.env.local

# 공공데이터 원본 — 용량이 커서 커밋하지 않는다 (스크립트로 다시 받을 수 있음)
data/raw/

# 확장 식품 데이터 — 35 MB. 배포 방식을 정한 뒤 포함한다.
# scripts/build-food-db.mjs 로 언제든 다시 만들 수 있다.
public/data/foods-extended.json
