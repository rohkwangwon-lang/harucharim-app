# 지난판

이 안의 파일들은 **setup.sql 하나로 합쳐졌습니다.** 실행하지 마세요.

나눠 두었더니 순서를 지켜야 하는 곳이 헷갈렸습니다 —
`admin2.sql` 의 `of_is_admin` 이 `admin.sql` 의 것을 덮어써야 하고,
`stats.sql` 안에서도 `of_return_rate` 가 `of_stat_return` 보다 먼저 와야 합니다.
순서를 틀리면 설치가 통째로 멈춥니다.

무엇이 어떻게 바뀌었는지 되짚을 때만 보세요.
